@model CosmeticsStore.Models.About

@{
    ViewBag.Title = "Chi tiết bài giới thiệu";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h1 class="h3 mb-2 text-gray-800">Bài giới thiệu </h1>
        <div>
            <a href="@Url.Action("Edit","Abouts" , new { @id = Model.AboutID})" class="btn btn-warning btn-icon-split">
                <span class="icon text-white-50">
                    <i class="fas fa-edit"></i>
                </span>
                <span class="text">Thay đổi</span>
            </a>
        </div>
        <div>
            <a href="@Url.Action("Index","Abouts")" class="btn btn-dark btn-icon-split">
                <span class="icon text-white-50">
                    <i class="fas fa-arrow-left"></i>
                </span>
                <span class="text">Trở lại</span>
            </a>
        </div>
    </div>
</div>

<div class="postWrap">
    <h1 class="post-heading"> @Model.Name</h1>
    @Html.Raw(Model.Content)
</div>




